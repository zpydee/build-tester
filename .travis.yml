language: node_js

node_js:
  - "7.8"

branches:
 only:
   - master


before_install:
  - npm install -g mocha chai

install:
  - npm install;

script:
  - npm run test;

after_success:
  - curl -H "Content-Type: application/json" --data '{"docker_tag": "master"}' -X POST $DOCKER_BUILD_URL
#   - if [ "$TRAVIS_BRANCH" == "master" ]; then
#    docker build -t irdt/cerebro .;
#    docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
#    docker push irdt/cerebro;
#    fi