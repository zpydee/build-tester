language: node_js

node_js:
  - "7.8"

branches:
 only:
   - master


before_install:
  - npm install -g mocha chai

install:
  - npm install;

script:
  - npm run test;

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    curl -H "Content-Type: application/json" --data '{"docker_tag": "master"}' -X POST https://registry.hub.docker.com/u/zpydee/build-tester/trigger/e01d5259-aafa-4ac2-a48e-dd950389ce36/
#    docker build -t irdt/cerebro .;
#    docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
#    docker push irdt/cerebro;
   fi